<%= form_for(@note) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= link_to 'Add poll', new_poll_path %>
    <%= f.text_area :title, placeholder: "Note Title..." %>
    <%= f.text_area :content, placeholder: "Compose new Note..." %>
  </div>
  <% if u == 0 %>
    <%= render 'permission_new', f: f %>
  <% else %>
    Edit User Permissions
    <%= render 'permission_edit', f: f, note: @note %>
    <%= f.hidden_field :lastEditedBy, value: current_user.id %>
  <% end %>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
