<% provide(:title, 'Edit a Comment') %>
<h1>Edit Comment</h1>

<%= form_for([@note, @comment]) do |f| %>

  <div class="field">
    <%= f.text_area :reply, placeholder: "Comment on this Note..." %>
  </div>

  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
